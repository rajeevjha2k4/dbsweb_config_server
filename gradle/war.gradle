apply plugin: "war"

bootWar {
    mainClassName = "com.dbs.common.config.ConfigserverApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
